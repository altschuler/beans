# TODO

- Consider moving `/transactions` actions into the shell header via a portal/header-actions slot. Keep the action logic owned by the transactions dashboard, but render the review count, Auto-categorize, sync, and more menu right-aligned in the breadcrumb header instead of in-page.
