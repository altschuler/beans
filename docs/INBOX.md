# Inbox

- 2026-09-08: Model mocks are now opt-in (`FLUE_MOCK=1`); normal Flue execution uses `OPENAI_API_KEY` and optional `FLUE_MODEL`. Orb services deliberately retain a scrubbed environment plus mock mode. The provider tests intercept OpenAI HTTP transport to cover real registration/auth/stream parsing without spending tokens; missing credentials must fail rather than silently fall back to a mock.
- 2026-09-08: Zero's browser 401 is resolved by same-origin `/zero` WebSocket proxying: Secure Better Auth cookies were not reaching the old cross-origin HTTP cache connection. Keep query/mutate cookie forwarding enabled; callbacks still authenticate the session normally. Verified real signed-in personal-team/category data in the orb. Public portal origin is supplied at runtime, not saved in env files; Better Auth now honors its documented trusted-origin setting.
- 2026-09-08: Stage 3 uses the built Flue server for the orb, not its Vite dev server: Vite loads stale ignored `.env` files and exposes development source routes outside Hono middleware. The built server reads only its explicit credential-free environment and protects its entire HTTP surface. Vite web config reloads also drifted off the portal port; `strictPort` plus supervised restart keeps the port contract explicit.
- 2026-09-08: Flue v2.0.3 CLI scaffolds Node CLI-only persistence (`src/db.ts`) without `--deploy`. Installed `flue docs` is authoritative; pre-v2 web snippets are incompatible. Pi 0.83.0's public `fauxProvider` supplies an in-process model mock, so no mock HTTP server or provider secrets are needed. The scaffold's TypeScript 7 causes ESLint peer drift; pin 6.0.3 in Flue and the root lint workspace. Ignored retired app env/node_modules can survive orb snapshots: stage 2 discarded these before scaffolding rather than reusing them.
- 2026-09-07: Browser sign-in succeeds in the orb, but Zero custom-query calls return 401 during headless verification even with cookie forwarding configured. This blocks live finance-data interaction checks; shell rendering and PostgreSQL-backed tests still work. Fixed the stale dev reset-recipe assertion to match synthetic `just seed`. Historical `confirmed` status is not evidence of human confirmation; status rendering must use confirmation metadata instead.
- 2026-09-07: Runtime removal exposed a stale second-app env copy in `.agents/setup`; setup now prepares only the web app. Retiring a package requires auditing setup and generated local env keys, not just workspace manifests. Applied migrations and historical financial provenance must remain separate from disposable runtime state.
- 2026-09-07: `pnpm knip` in this orb exits inside oxc-parser with `RangeError: Array buffer allocation failed` before reporting results. Targeted ESLint and the PostgreSQL-backed seed tests run successfully; investigate parser memory requirements before treating Knip as an available verification gate here.
- 2026-07-11: `AGENTS.md` still names missing `docs/FRONTEND.md`, `docs/I18N.md`, and `docs/DATABASES.md`; the available database source is `docs/DATABASE.md`. Use the available reference/source docs until these names are reconciled.
- 2026-07-10: A successful Playwright run can terminate its `just dev` web-server command while leaving Zero's change-streamer process orphaned under PID 1. A second e2e/check run then fails because the managed change-streamer port is occupied. Make the dev/e2e shutdown path terminate the full Zero process tree.
- 2026-07-10: `scripts/dev.mjs init` preserves keys already present in the managed root and app env files even after those keys are removed from `dev.config.mjs`. Runtime cutovers therefore require manual cleanup of obsolete generated keys; add a declarative retired-key mechanism so regeneration can remove them safely.
- 2026-07-10: The Playwright harness computes `baseURL` from the parent process (`https://localhost:3100` fallback), while `just dev` loads generated `apps/web/.env` and may start Vite on a different managed port (3101 in this checkout). The web-server readiness check then times out even though Vite started. This run also found the generated Zero change-streamer port 5001 already occupied. Make Playwright load the managed env/port allocation before treating e2e as a reliable acceptance gate.
- 2026-07-10: `drizzle-kit generate` requires a TTY when one migration both drops and adds columns because it prompts for create-vs-rename decisions; non-interactive agent/CI execution exits before generation. Run it in an interactive terminal (choosing “create” for genuinely new columns) or automate the prompts with a PTY/`expect`; a documented non-interactive generator wrapper would remove this friction.
- 2026-07-09: Zero singular (`one(...)`) relationships break the sync connection ("Singular relationship 'posting' should not have multiple rows" → poke error → disconnect) whenever the child row behind the relationship is replaced under a new primary key — Zero applies row diffs one at a time in key order, so an add can land before the matching delete. Keep categorization safe by creating the bank-linked posting during import and preserving that row on every category/split/transfer rewrite. Rows not behind a singular relationship (the category/explanatory postings, `bankTransactionId` null) can churn with any ids.
- 2026-07-07: Pi reviewer/delegate subagents timed out repeatedly on a concise changed-file review without producing a useful transcript. If this repeats during mandatory review, do a local read-only review and note the timeout rather than waiting on more retries.
- 2026-07-07: Adding a new Zero-synced app table requires both `drizzle-zero.config.ts`/`zero:generate` and an explicit publication migration for `penge_zero_app`; Drizzle schema generation creates the table migration but does not update the publication automatically.
- 2026-06-27: The copied toolbox `scripts/dev.mjs` checks `git check-ignore -q .worktrees` before creating the directory. A `.gitignore` entry of only `.worktrees/` may not satisfy that check before the directory exists, so keep both `.worktrees` and `.worktrees/` ignored or adjust the toolbox check if this becomes surprising.
- 2026-06-25: Local dev port defaults are scattered across package scripts, Vite config, env examples, test setup, Playwright, app URL fallbacks, and docs. `docs/TODO.md` now lists the concrete locations; future work should centralize these before the next port move.
- 2026-06-25: `pnpm check` currently reaches Playwright then fails in `e2e/smoke.spec.ts` because `/login` never exposes `data-testid="auth-toggle"` within 10s. Lint, typecheck, and unit tests can pass before this, so verify/fix the login smoke separately before using `pnpm check` as a clean merge gate.
- 2026-06-25: Pi subagent async `context-builder` fanout for Section 3 domain extraction failed immediately with stale-run reconciliation before writing child results. If this repeats, skip the fanout and inspect locally rather than waiting on status polling.
- 2026-06-25: `drizzle-zero generate` marks DB-default columns as optional in the generated client schema and warns that Zero clients cannot rely on database defaults. When adding synced columns with defaults, optimistic mutators must set/update them explicitly and type code should handle `null`/missing local values.
- 2026-06-22: Pi subagent async runs can fill `/tmp` quickly; this run exhausted the 30G tmpfs under `/tmp/pi-subagents-uid-1000`. If subagent status calls fail with `ENOSPC`, clear stale files under that Pi temp directory before continuing verification.
- 2026-06-22: Pi subagent acceptance rejected worker outputs even though reports were saved and validation passed because the runtime did not detect the fenced `acceptance-report`. Prefer no/built-in acceptance gates or verify reports manually until the expected structured-output format is documented.
- 2026-06-21: `tests/unit/category-management-server.test.ts` (and likely other Postgres-backed unit tests) can flake when `pnpm test` runs the whole suite in parallel — they share one test DB and contend on migrations/rows, producing inconsistent failures that vanish when the file runs in isolation. Consider giving DB-backed suites a serialized pool or per-suite schema/transaction isolation before relying on `pnpm test` as a merge gate.
- 2026-06-21: `pnpm test:e2e` smoke currently expects the banking dashboard at `/app`, but `/app` now renders the intentional welcome page and the banking dashboard lives at `/app/banks`. Update the smoke flow or route expectation before using e2e as a merge gate.
- 2026-06-20: Vite 8 warns that `vite-tsconfig-paths` is now redundant because `resolve.tsconfigPaths: true` is built in. Consider replacing the plugin to remove repeated build/test warnings.
- 2026-06-20: `AGENTS.md` still points to several missing docs (`docs/FRONTEND.md`, `docs/I18N.md`, `docs/ENVIRONMENT.md`, `docs/TESTING.md`, `docs/CODESTYLE.md`, `docs/DATABASES.md`) while this checkout has only some renamed/partial equivalents. Agents changing frontend/auth/test areas need to fall back to available docs and source until the references are reconciled.
- 2026-06-19: Database-backed unit tests must never point at the dev database. Test setup now requires `TEST_DATABASE_URL`, copies it into `DATABASE_URL`/`ZERO_UPSTREAM_DB`, and destructive helpers refuse DB names that do not look test-only. Use manual dev DB resets only when explicitly intended.
- 2026-06-18: Ledger dashboard categorization work showed that `pnpm test`, focused tests, typecheck, and lint can all pass while `pnpm build` fails TanStack Start import protection for client-reachable dynamic imports of `*.server.*` modules. Consider adding `pnpm build` to plans/checks that introduce server-only modules used near client/shared boundaries.
- 2026-06-18: `AGENTS.md` references `docs/DATABASES.md`, `docs/TESTING.md`, `docs/CODESTYLE.md`, and `docs/SERVER.md`, but this checkout currently has `docs/DATABASE.md` and no testing/code-style/server docs. Future agents will hit missing-doc reads unless the references or docs are reconciled.
- 2026-06-18: Envelope ledger foundation creates ledger rows for new bank syncs, but historical bank transactions that already exist before the migration need an explicit backfill/reconciliation strategy. This may belong with the planned reconciliation-problem reporting follow-up.
- 2026-06-18: Ledger DB constraints currently rely on repository code for same-team integrity between ledger transactions, bank transactions, and movement accounts. Consider composite keys/checks or validation when manual ledger editing is introduced.
- 2026-09-06: Orb setup must not call `just setup`/`just init`: these reset Docker volumes and require an untracked private seed dump. `.agents/setup` instead migrates an isolated native PostgreSQL 18 cluster. The orb template's pnpm 12 also fails on this repo's unspecified dependency build policies; the orb setup pins pnpm 10.34.5 and preserves the existing approval policy.
